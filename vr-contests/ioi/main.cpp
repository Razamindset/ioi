#include <iostream>
#include <vector>
using namespace std;

int main() {
  int l1, n, l2, m, limit, games;

  cin >> l1 >> n >> l2 >> m >> limit >> games;

  int lenSeg, numInSeg;
  cin >> lenSeg >> numInSeg;

  vector<int> a(l1);
  vector<int> b(l2);
  vector<int> c;

  return 0;
}